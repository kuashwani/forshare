Install :
  $ wget https://go.dev/dl/go1.18.1.linux-amd64.tar.gz
  $ sudo tar -zxvf go1.18.1.linux-amd64.tar.gz -C /usr/local/
  $ echo 'export GOROOT=/usr/local/go' >> ~/.bashrc &&   echo 'export GOPATH=$HOME/go' >> ~/.bashrc &&   echo 'export PATH=$PATH:$GOROOT/bin:$GOPATH/bin' >> ~/.bashrc
  $ go version
  $ go install github.com/go-delve/delve/cmd/dlv@latest
  
  Uninstall :
  $ which go
  /usr/local/go/bin/go
  $ sudo rm -rf /usr/local/go
  $ sudo rm /etc/paths.d/go --> seems like doesn't exists
  $ Remove go entries from .bashrc.
